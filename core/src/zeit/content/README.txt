===========
CMS Content
===========


CMS content is usually stored in XML format plus additional metadata. The
metadata itself is not necessarily stored in the XML document. The CMS needs to
map the data to Python objects and vice versa.
